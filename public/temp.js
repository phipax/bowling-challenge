	if(this.strike) {
		x = Object.entries(game._frames);
		gscore = 0;
		x.forEach((item,key) => {
			var y = Object.entries(item);    
			y.forEach((i,k) => { i
				f(k==='score'){
					gscore += i;
				 } 
			}); 
		});
	}
